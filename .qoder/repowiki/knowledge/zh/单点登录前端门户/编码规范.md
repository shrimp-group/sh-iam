- API 请求统一通过 `@/utils/request` 封装的 Axios 实例发起，并在拦截器中自动注入 `token` 和 `app-code` 头。
- 敏感字段（如登录密码）在提交前使用 `jsencrypt` 进行 RSA 加密处理。
- 全局组件（如 Pagination, DictTag, FileUpload）在 `main.js` 中统一注册，便于在模板中直接使用。
- 路由跳转前通过 `permission.js` 守卫检查 Token 有效性，未登录用户强制重定向至 `/login`。