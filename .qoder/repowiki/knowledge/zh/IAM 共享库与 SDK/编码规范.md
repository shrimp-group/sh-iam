- DTO 与 Entity 严格分离，在 `iam-common` 中分别存放于 `dto` 和 `entity` 包下，且命名遵循 `Iam[Domain]Dto` 与 `Iam[Domain]` 规范。
- 安全相关工具类（如 `PasswordHelper`, `JwtUtil`）采用静态方法设计，不依赖 Spring 容器，便于在非 Web 环境下复用。
- SDK 配置项统一收敛于 `IamSdkConfig`，并通过 `@Value` 注入默认值，确保在未显式配置时仍具备基础运行能力。