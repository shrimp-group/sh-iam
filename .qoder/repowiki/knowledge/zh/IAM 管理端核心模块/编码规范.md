- REST 控制器统一继承或引用 `Route` 接口常量来定义 URL 路径，确保路由集中管理。
- Service 层普遍继承 `BaseService<Entity, Mapper>` 基类，复用通用的 CRUD 与分页查询能力。
- 复杂业务操作（如用户创建）在 Service 层使用 `@Transactional` 保证用户主表、认证表及密码历史表的数据一致性。
- Mapper 接口与 XML 文件严格保持同名对应，且 XML 中 SQL 语句通常包含 `deleted = 0` 以实现逻辑删除过滤。