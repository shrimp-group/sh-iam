- 敏感数据传输加密：登录与改密接口中，若配置了私钥且密码长度超过阈值，则使用RSA私钥对前端传来的密码进行解密处理。
- 统一响应封装：所有REST接口均返回`com.wkclz.core.base.R`包装对象，确保前后端交互格式的一致性。
- 登录状态枚举化：登录逻辑中广泛使用`LoginStatus`枚举来标识不同的认证结果（如用户不存在、密码错误、需要验证码等），并记录详细日志。
- 会话并发控制：在登录成功后，通过Redis ZSet维护用户会话列表，并根据配置的最大并发数自动踢出最早的会话。