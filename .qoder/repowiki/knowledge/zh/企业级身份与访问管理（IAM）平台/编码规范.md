- 后端实体类统一采用 `Iam` 前缀命名（如 `IamUser`），控制器类采用 `业务名 + Rest` 命名（如 `UserRest`）。
- 数据库表名统一使用 `iam_` 前缀，且所有实体必须包含审计字段（create_time, update_by 等）及逻辑删除标记。
- 前端 API 调用层按业务域划分目录（如 `src/api/system/`, `src/api/user/`），并统一使用 `request.js` 封装的 Axios 实例。
- Git 提交信息严格遵循 `类型(模块): 描述` 格式（如 `feat(iam-sso): 新增短信验证码登录`）。