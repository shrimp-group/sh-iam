# sh-auth 公共认证模块实现计划

> **面向 AI 代理的工作者：** 使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 从零构建 sh-auth 框架级认证-授权-鉴权模块，零 IAM 依赖，通过 SPI 契约支持多种认证方式和授权体系。

**架构：** 25 个 SPI 契约接口 + 5 个过滤器 + 19 个数据模型（含 AuthMetadata 应用级单例 + SubjectAuthorization 用户级轻量缓存快照）+ SecurityContext + ShAuthAutoConfiguration。过滤器链：RequestWrapper → RequestLog（最外层，含 finally clear）→ SecurityHeader → Authentication → Authorization。LoginService 模板方法编排登录流程。

**技术栈：** Java 25, Spring Boot 3.x, Maven, sh-parent BOM, sh-web, sh-redis (optional), jakarta-servlet-api, slf4j, Lombok

**规格文档：** `docs/superpowers/specs/2026-07-11-sh-auth-design.md`

---

## 文件结构总览

```
sh-auth/
├── pom.xml
├── src/main/java/com/wkclz/auth/
│   ├── ShAuthAutoConfiguration.java
│   ├── config/
│   │   ├── AuthProperties.java
│   │   └── AuthConstants.java
│   ├── context/
│   │   └── SecurityContext.java
│   ├── enums/                    (8 个枚举)
│   │   ├── AccountStatus.java
│   │   ├── AuthErrorType.java
│   │   ├── AuthStatus.java
│   │   ├── CredentialType.java
│   │   ├── FieldPermissionType.java
│   │   ├── MenuType.java
│   │   ├── MfaType.java
│   │   └── TokenType.java
│   ├── exception/                (7 个异常)
│   │   ├── AccountStatusException.java
│   │   ├── AuthenticationException.java
│   │   ├── AuthorizationException.java
│   │   ├── AuthException.java
│   │   ├── MfaRequiredException.java
│   │   ├── RateLimitException.java
│   │   └── SessionExpiredException.java
│   ├── model/                    (19 个模型)
│   │   ├── ApiField.java
│   │   ├── ApiResource.java
│   │   ├── AuthMetadata.java
│   │   ├── AuthRequest.java
│   │   ├── AuthResult.java
│   │   ├── AuthToken.java
│   │   ├── Captcha.java
│   │   ├── Credential.java
│   │   ├── DataScope.java
│   │   ├── FieldPermission.java
│   │   ├── LoginRecord.java
│   │   ├── MenuNode.java
│   │   ├── MfaChallenge.java
│   │   ├── Permission.java
│   │   ├── Principal.java
│   │   ├── RequestRecord.java
│   │   ├── Role.java
│   │   ├── RoleDataScope.java
│   │   ├── SecurityHeaders.java
│   │   ├── Session.java
│   │   ├── Subject.java
│   │   ├── SubjectAuthorization.java
│   │   └── SubjectRole.java
│   ├── contract/
│   │   ├── auth/                 (13 个认证 SPI)
│   │   │   ├── AccountStatusChecker.java
│   │   │   ├── AuthenticationProvider.java
│   │   │   ├── CaptchaService.java
│   │   │   ├── ConcurrentSessionControl.java
│   │   │   ├── CredentialProvider.java
│   │   │   ├── DefaultLogoutService.java
│   │   │   ├── LoginService.java
│   │   │   ├── LogoutService.java
│   │   │   ├── MfaService.java
│   │   │   ├── OAuthProvider.java
│   │   │   ├── PasswordEncoder.java
│   │   │   ├── PasswordValidator.java
│   │   │   ├── RateLimitChecker.java
│   │   │   ├── SessionStore.java
│   │   │   ├── SmsCodeSender.java
│   │   │   └── TokenService.java
│   │   ├── authz/                (4 个授权 SPI)
│   │   │   ├── AccessControlProvider.java
│   │   │   ├── DataScopeProvider.java
│   │   │   ├── FieldPermissionProvider.java
│   │   │   └── MenuProvider.java
│   │   └── infra/                (2 个基础设施 SPI)
│   │       ├── RequestLogger.java
│   │       └── SecurityHeaderProvider.java
│   └── filter/                   (6 个过滤器)
│       ├── AuthenticationFilter.java
│       ├── AuthorizationFilter.java
│       ├── EagerContentCachingRequestWrapper.java
│       ├── RequestLogFilter.java
│       ├── RequestWrapperFilter.java
│       └── SecurityHeaderFilter.java
├── src/main/resources/
│   └── META-INF/spring/
│       └── org.springframework.boot.autoconfigure.AutoConfiguration.imports
└── src/test/java/com/wkclz/auth/
    ├── filter/
    │   └── FilterChainTest.java
    ├── context/
    │   └── SecurityContextTest.java
    └── contract/
        └── LoginServiceTest.java
```

---

### 任务 1：模块骨架 — pom.xml + 目录结构

**文件：**
- 创建：`sh-auth/pom.xml`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/` （所有子包目录）

- [ ] **步骤 1：创建 pom.xml**

```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.wkclz.framework</groupId>
        <artifactId>sh-parent</artifactId>
        <version>5.0.0-SNAPSHOT</version>
        <relativePath/>
    </parent>

    <artifactId>sh-auth</artifactId>
    <version>5.1.0-SNAPSHOT</version>
    <packaging>jar</packaging>
    <name>sh-auth</name>
    <description>公共认证-授权-鉴权模块</description>

    <dependencies>
        <!-- sh-web: IpHelper, RequestHelper, R -->
        <dependency>
            <groupId>com.wkclz.framework</groupId>
            <artifactId>sh-web</artifactId>
        </dependency>

        <!-- sh-redis: RedisTemplate (optional) -->
        <dependency>
            <groupId>com.wkclz.framework</groupId>
            <artifactId>sh-redis</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Servlet API -->
        <dependency>
            <groupId>jakarta.servlet</groupId>
            <artifactId>jakarta.servlet-api</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- Spring Boot Autoconfigure -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-autoconfigure</artifactId>
        </dependency>

        <!-- SLF4J -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
```

- [ ] **步骤 2：创建包目录结构**

```bash
mkdir -p sh-auth/src/main/java/com/wkclz/auth/{config,context,enums,exception,model,contract/{auth,authz,infra},filter}
mkdir -p sh-auth/src/main/resources/META-INF/spring
mkdir -p sh-auth/src/test/java/com/wkclz/auth/{filter,context,contract}
```

- [ ] **步骤 3：验证模块编译**

```bash
mvn compile -pl sh-auth -am
```

预期：BUILD SUCCESS（仅有空目录，无 Java 文件，编译通过）

- [ ] **步骤 4：Commit**

```bash
git add sh-auth/pom.xml sh-auth/src/
git commit -m "feat: add sh-auth module skeleton with pom.xml and directory structure"
```

---

### 任务 2：配置与常量

**文件：**
- 创建：`sh-auth/src/main/java/com/wkclz/auth/config/AuthConstants.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/config/AuthProperties.java`

- [ ] **步骤 1：编写 AuthConstants**

```java
package com.wkclz.auth.config;

/**
 * sh-auth 常量定义
 */
public final class AuthConstants {

    /** 默认 Token 请求头 */
    public static final String DEFAULT_TOKEN_HEADER = "Authorization";

    /** Bearer Token 前缀 */
    public static final String BEARER_PREFIX = "Bearer ";

    /** 自定义 Token 请求头（兼容） */
    public static final String CUSTOM_TOKEN_HEADER = "token";

    /** 频率限制 Redis Key 前缀 */
    public static final String RATE_LIMIT_KEY_PREFIX = "auth:rate_limit:";

    /** 验证码 Redis Key 前缀 */
    public static final String CAPTCHA_KEY_PREFIX = "auth:captcha:";

    /** Session Redis Key 前缀 */
    public static final String SESSION_KEY_PREFIX = "auth:session:";

    /** 用户会话列表 Redis Key 前缀 */
    public static final String SESSION_LIST_KEY_PREFIX = "auth:session:list:";

    /** MFA 挑战 Redis Key 前缀 */
    public static final String MFA_CHALLENGE_KEY_PREFIX = "auth:mfa:challenge:";

    private AuthConstants() {
    }
}
```

- [ ] **步骤 2：编写 AuthProperties**

```java
package com.wkclz.auth.config;

import com.wkclz.auth.enums.MfaType;
import lombok.Data;
import org.springframework.boot.context.properties.ConfigurationProperties;

import java.util.List;

/**
 * sh-auth 配置属性
 */
@Data
@ConfigurationProperties(prefix = "sh.auth")
public class AuthProperties {

    /** 是否启用 */
    private boolean enabled = true;

    /** 会话配置 */
    private Session session = new Session();

    /** 密码策略 */
    private Password password = new Password();

    /** MFA 配置 */
    private Mfa mfa = new Mfa();

    /** 频率限制 */
    private RateLimit rateLimit = new RateLimit();

    /** 白名单 */
    private WhiteList whiteList = new WhiteList();

    /** CORS 配置 */
    private Cors cors = new Cors();

    @Data
    public static class Session {
        private long ttl = 86400;
        private int maxConcurrent = 0;
    }

    @Data
    public static class Password {
        private int expireDays = 180;
        private int historySize = 5;
        private int minLength = 8;
    }

    @Data
    public static class Mfa {
        private MfaType defaultType = null;
    }

    @Data
    public static class RateLimit {
        private int maxAttempts = 5;
        private int windowMinutes = 60;
    }

    @Data
    public static class WhiteList {
        private List<String> paths = List.of(
            "/public/**",
            "/actuator/**",
            "/error"
        );
    }

    @Data
    public static class Cors {
        private boolean enabled = false;
        private String allowedOrigins = "*";
        private String allowedMethods = "GET,POST,PUT,DELETE,OPTIONS";
        private String allowedHeaders = "*";
        private long maxAge = 3600;
    }
}
```

- [ ] **步骤 3：Commit**

```bash
git add sh-auth/src/main/java/com/wkclz/auth/config/
git commit -m "feat: add AuthConstants and AuthProperties configuration"
```

---

### 任务 3：枚举定义

**文件：**
- 创建：`sh-auth/src/main/java/com/wkclz/auth/enums/AccountStatus.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/enums/AuthErrorType.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/enums/AuthStatus.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/enums/CredentialType.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/enums/FieldPermissionType.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/enums/MenuType.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/enums/MfaType.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/enums/TokenType.java`

- [ ] **步骤 1：一次编写全部 8 个枚举**

`AccountStatus.java`：

```java
package com.wkclz.auth.enums;

/** 账号状态 */
public enum AccountStatus {
    ENABLED,
    DISABLED,
    LOCKED
}
```

`AuthErrorType.java`：

```java
package com.wkclz.auth.enums;

/** 认证错误类型 */
public enum AuthErrorType {
    BAD_CREDENTIALS,
    USER_NOT_FOUND,
    ACCOUNT_LOCKED,
    ACCOUNT_DISABLED,
    ACCOUNT_EXPIRED,
    CREDENTIALS_EXPIRED,
    TOKEN_EXPIRED,
    TOKEN_INVALID,
    CAPTCHA_REQUIRED,
    CAPTCHA_ERROR,
    CAPTCHA_TIMEOUT,
    MFA_REQUIRED,
    MFA_ERROR,
    RATE_LIMITED,
    SESSION_EXPIRED,
    ACCESS_DENIED,
    INTERNAL_ERROR,
    SERVICE_UNAVAILABLE
}
```

`AuthStatus.java`：

```java
package com.wkclz.auth.enums;

/** 认证状态 */
public enum AuthStatus {
    SUCCESS,
    MFA_REQUIRED,
    FAILED
}
```

`CredentialType.java`：

```java
package com.wkclz.auth.enums;

/** 凭证类型 */
public enum CredentialType {
    PASSWORD,
    SMS_CODE,
    SOCIAL_CODE
}
```

`FieldPermissionType.java`：

```java
package com.wkclz.auth.enums;

/** 字段权限类型 */
public enum FieldPermissionType {
    READ,
    WRITE,
    HIDDEN
}
```

`MenuType.java`：

```java
package com.wkclz.auth.enums;

/** 菜单类型 */
public enum MenuType {
    MENU,
    BUTTON
}
```

`MfaType.java`：

```java
package com.wkclz.auth.enums;

/** MFA 类型 */
public enum MfaType {
    SMS,
    TOTP
}
```

`TokenType.java`：

```java
package com.wkclz.auth.enums;

/** Token 类型 */
public enum TokenType {
    JWT,
    OAUTH_BEARER,
    SESSION_ID
}
```
- [ ] **步骤 2：编写缓存模型**

`AuthMetadata.java`（应用级单例）：

```java
package com.wkclz.auth.model;

import lombok.Data;
import java.io.Serializable;
import java.time.LocalDateTime;
import java.util.List;
import java.util.Map;

/** 应用级 RBAC 缓存快照（单例，所有用户共享） */
@Data
public class AuthMetadata implements Serializable {
    private String appCode;
    private Map<String, Role> roles;
    private Map<String, MenuNode> menus;
    private Map<String, ApiResource> apis;
    private Map<String, List<String>> roleMenus;
    private Map<String, List<String>> menuApis;
    private Map<String, List<ApiField>> apiFields;
    private LocalDateTime loadTime;
}
```

`SubjectAuthorization.java`（用户级轻量缓存）：

```java
package com.wkclz.auth.model;

import lombok.Data;
import java.io.Serializable;
import java.time.LocalDateTime;
import java.util.List;
import java.util.Map;

/** 用户级授权缓存快照（每用户一份，仅角色码 + 数据权限） */
@Data
public class SubjectAuthorization implements Serializable {
    private String subjectId;
    private List<SubjectRole> roles;
    private Map<String, List<RoleDataScope>> roleDataScopes;
    private LocalDateTime loadTime;
}
```

- [ ] **步骤 3：验证编译**

```bash
mvn compile -pl sh-auth -am
```

预期：BUILD SUCCESS

- [ ] **步骤 3：Commit**

```bash
git add sh-auth/src/main/java/com/wkclz/auth/enums/
git commit -m "feat: add all 8 auth enums (AccountStatus, AuthErrorType, AuthStatus, CredentialType, FieldPermissionType, MenuType, MfaType, TokenType)"
```

---

### 任务 4：异常体系

**文件：**
- 创建：`sh-auth/src/main/java/com/wkclz/auth/exception/AuthException.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/exception/AuthenticationException.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/exception/AuthorizationException.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/exception/SessionExpiredException.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/exception/AccountStatusException.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/exception/MfaRequiredException.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/exception/RateLimitException.java`

- [ ] **步骤 1：编写基类 AuthException**

```java
package com.wkclz.auth.exception;

import com.wkclz.auth.enums.AuthErrorType;
import lombok.Getter;

/**
 * 认证/授权异常基类
 */
@Getter
public class AuthException extends RuntimeException {

    private final AuthErrorType errorType;

    public AuthException(AuthErrorType errorType, String message) {
        super(message);
        this.errorType = errorType;
    }

    public AuthException(AuthErrorType errorType, String message, Throwable cause) {
        super(message, cause);
        this.errorType = errorType;
    }
}
```

- [ ] **步骤 2：编写 6 个子类异常（一次性写入全部文件）**

`AuthenticationException.java`：

```java
package com.wkclz.auth.exception;

import com.wkclz.auth.enums.AuthErrorType;

/** 认证异常 */
public class AuthenticationException extends AuthException {
    public AuthenticationException(AuthErrorType errorType, String message) {
        super(errorType, message);
    }

    public AuthenticationException(AuthErrorType errorType, String message, Throwable cause) {
        super(errorType, message, cause);
    }
}
```

`AuthorizationException.java`：

```java
package com.wkclz.auth.exception;

import com.wkclz.auth.enums.AuthErrorType;

/** 授权异常 */
public class AuthorizationException extends AuthException {
    public AuthorizationException(String message) {
        super(AuthErrorType.ACCESS_DENIED, message);
    }
}
```

`SessionExpiredException.java`：

```java
package com.wkclz.auth.exception;

import com.wkclz.auth.enums.AuthErrorType;

/** 会话过期异常 */
public class SessionExpiredException extends AuthException {
    public SessionExpiredException(String message) {
        super(AuthErrorType.SESSION_EXPIRED, message);
    }
}
```

`AccountStatusException.java`：

```java
package com.wkclz.auth.exception;

import com.wkclz.auth.enums.AuthErrorType;

/** 账号状态异常 */
public class AccountStatusException extends AuthException {
    public AccountStatusException(AuthErrorType errorType, String message) {
        super(errorType, message);
    }
}
```

`MfaRequiredException.java`：

```java
package com.wkclz.auth.exception;

import com.wkclz.auth.enums.AuthErrorType;
import lombok.Getter;

/** MFA 要求异常（携带 challengeId 供全局异常处理器映射） */
@Getter
public class MfaRequiredException extends AuthException {

    private final String challengeId;

    public MfaRequiredException(String challengeId, String message) {
        super(AuthErrorType.MFA_REQUIRED, message);
        this.challengeId = challengeId;
    }
}
```

`RateLimitException.java`：

```java
package com.wkclz.auth.exception;

import com.wkclz.auth.enums.AuthErrorType;

/** 频率限制异常 */
public class RateLimitException extends AuthException {
    public RateLimitException(String message) {
        super(AuthErrorType.RATE_LIMITED, message);
    }
}
```

- [ ] **步骤 3：验证编译**

```bash
mvn compile -pl sh-auth -am
```

预期：BUILD SUCCESS

- [ ] **步骤 4：Commit**

```bash
git add sh-auth/src/main/java/com/wkclz/auth/exception/
git commit -m "feat: add exception hierarchy (AuthException base + 6 subclasses)"
```

---

### 任务 5：模型 — 核心实体

**文件：**
- 创建：`sh-auth/src/main/java/com/wkclz/auth/model/Principal.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/model/Subject.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/model/Credential.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/model/AuthRequest.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/model/AuthResult.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/model/AuthToken.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/model/Session.java`

- [ ] **步骤 1：编写 Principal**

```java
package com.wkclz.auth.model;

import lombok.Data;
import java.io.Serializable;

/** 用户主体 */
@Data
public class Principal implements Serializable {
    private String userCode;
    private String username;
    private String nickname;
    private String avatar;
    private String tenantCode;
    private String appCode;
    private String authIdentifier; // 认证标识（用户名/手机号/openId，按 authType 区分含义，可选）
}
```

- [ ] **步骤 2：编写 Subject**

```java
package com.wkclz.auth.model;

import com.wkclz.auth.enums.AccountStatus;
import lombok.Data;
import java.io.Serializable;
import java.time.LocalDateTime;

/** 认证主体（账号实体） */
@Data
public class Subject implements Serializable {
    private String subjectId;
    private String authType;
    private String authIdentifier;
    private AccountStatus status;
    private LocalDateTime expireTime;
}
```

- [ ] **步骤 3：编写 Credential**

```java
package com.wkclz.auth.model;

import com.wkclz.auth.enums.CredentialType;
import lombok.Data;
import java.io.Serializable;

/** 凭据 */
@Data
public class Credential implements Serializable {
    private CredentialType type;
    private String credentialValue;
    private String captchaCode;
    private String captchaId;
}
```

- [ ] **步骤 4：编写 AuthRequest**

```java
package com.wkclz.auth.model;

import lombok.Data;
import java.io.Serializable;
import java.util.Map;

/** 认证请求 */
@Data
public class AuthRequest implements Serializable {
    private String authType;
    private Credential credential;
    private Map<String, Object> extra;
}
```

- [ ] **步骤 5：编写 AuthToken**

```java
package com.wkclz.auth.model;

import com.wkclz.auth.enums.TokenType;
import lombok.Data;
import java.io.Serializable;
import java.time.LocalDateTime;

/** 认证令牌 */
@Data
public class AuthToken implements Serializable {
    private TokenType type;
    private String tokenValue;
    private LocalDateTime expireTime;
    private LocalDateTime issueTime;
}
```

- [ ] **步骤 6：编写 AuthResult**

```java
package com.wkclz.auth.model;

import com.wkclz.auth.enums.AuthErrorType;
import com.wkclz.auth.enums.AuthStatus;
import lombok.Data;
import java.io.Serializable;

/** 认证结果 */
@Data
public class AuthResult implements Serializable {
    private AuthStatus status;
    private AuthErrorType errorType;
    private String errorMessage;
    private AuthToken token;
    private Principal principal;
    private MfaChallenge mfaChallenge;

    public boolean isSuccess() {
        return status == AuthStatus.SUCCESS;
    }

    public static AuthResult success(Principal principal, AuthToken token) {
        AuthResult r = new AuthResult();
        r.status = AuthStatus.SUCCESS;
        r.principal = principal;
        r.token = token;
        return r;
    }

    public static AuthResult fail(AuthErrorType errorType, String message) {
        AuthResult r = new AuthResult();
        r.status = AuthStatus.FAILED;
        r.errorType = errorType;
        r.errorMessage = message;
        return r;
    }
}
```

- [ ] **步骤 7：编写 Session**

```java
package com.wkclz.auth.model;

import lombok.Data;
import java.io.Serializable;
import java.time.LocalDateTime;

/** 会话 */
@Data
public class Session implements Serializable {
    private String sessionId;
    private String subjectId;
    private Principal principal;
    private String authType;
    private String authIdentifier;
    private LocalDateTime createTime;
    private LocalDateTime expireTime;
    private String clientIp;
    private String userAgent;
}
```

- [ ] **步骤 8：验证编译**

```bash
mvn compile -pl sh-auth -am
```

预期：BUILD SUCCESS

- [ ] **步骤 9：Commit**

```bash
git add sh-auth/src/main/java/com/wkclz/auth/model/Principal.java sh-auth/src/main/java/com/wkclz/auth/model/Subject.java sh-auth/src/main/java/com/wkclz/auth/model/Credential.java sh-auth/src/main/java/com/wkclz/auth/model/AuthRequest.java sh-auth/src/main/java/com/wkclz/auth/model/AuthResult.java sh-auth/src/main/java/com/wkclz/auth/model/AuthToken.java sh-auth/src/main/java/com/wkclz/auth/model/Session.java
git commit -m "feat: add core models (Principal, Subject, Credential, AuthRequest, AuthResult, AuthToken, Session)"
```

---

### 任务 6：模型 — 认证域辅助模型

**文件：**
- 创建：`sh-auth/src/main/java/com/wkclz/auth/model/Captcha.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/model/MfaChallenge.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/model/ModelPermission.java` → 注意：这是 `Permission.java`（但 Permission 与 java.security.Permission 冲突，使用全限定名或重命名）

> **注意：** 为避免与 `java.security.Permission` 冲突，该模型类命名为 `Permission` 但需注意 import。如果冲突，可命名为 `AuthPermission`。

- [ ] **步骤 1：编写 Captcha**

```java
package com.wkclz.auth.model;

import lombok.Data;
import java.io.Serializable;
import java.time.LocalDateTime;

/** 图形验证码 */
@Data
public class Captcha implements Serializable {
    private String captchaId;
    private String captchaCode;
    private String captchaImage;
    private LocalDateTime expireTime;
}
```

- [ ] **步骤 2：编写 MfaChallenge**

```java
package com.wkclz.auth.model;

import com.wkclz.auth.enums.MfaType;
import lombok.Data;
import java.io.Serializable;
import java.time.LocalDateTime;

/** MFA 挑战 */
@Data
public class MfaChallenge implements Serializable {
    private String challengeId;
    private MfaType mfaType;
    private String target;
    private LocalDateTime expireTime;
    private boolean used;
}
```

- [ ] **步骤 3：编写 AuthPermission（权限定义）**

```java
package com.wkclz.auth.model;

import lombok.Data;
import java.io.Serializable;

/** 权限定义 */
@Data
public class AuthPermission implements Serializable {
    private String resource;
    private String action;
    private String description;
}
```

> 注意：命名为 `AuthPermission` 避免与 `java.security.Permission` 冲突。同时在 `AccessControlProvider.getUserPermissions()` 中返回类型也改用 `AuthPermission`。

- [ ] **步骤 4：验证编译**

```bash
mvn compile -pl sh-auth -am
```

预期：BUILD SUCCESS

- [ ] **步骤 5：Commit**

```bash
git add sh-auth/src/main/java/com/wkclz/auth/model/Captcha.java sh-auth/src/main/java/com/wkclz/auth/model/MfaChallenge.java sh-auth/src/main/java/com/wkclz/auth/model/AuthPermission.java
git commit -m "feat: add auth domain models (Captcha, MfaChallenge, AuthPermission)"
```

---

### 任务 7：模型 — RBAC 资源模型

**文件：**
- 创建：`sh-auth/src/main/java/com/wkclz/auth/model/Role.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/model/MenuNode.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/model/ApiResource.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/model/DataScope.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/model/FieldPermission.java`

- [ ] **步骤 1：编写全部 5 个 RBAC 资源模型**

`Role.java`：

```java
package com.wkclz.auth.model;

import lombok.Data;
import java.io.Serializable;

/** 角色 */
@Data
public class Role implements Serializable {
    private String roleCode;
    private String roleName;
    private String parentCode;
    private String appCode;
    private String tenantCode;
    private Boolean applicable;
}
```

`MenuNode.java`：

```java
package com.wkclz.auth.model;

import com.wkclz.auth.enums.MenuType;
import lombok.Data;
import java.io.Serializable;
import java.util.List;

/** 菜单节点 */
@Data
public class MenuNode implements Serializable {
    private String menuCode;
    private String parentCode;
    private String menuName;
    private MenuType menuType;
    private String routePath;
    private String component;
    private String buttonCode;
    private String appCode;
    private Integer sort;
    private List<MenuNode> children;
}
```

`ApiResource.java`：

```java
package com.wkclz.auth.model;

import lombok.Data;
import java.io.Serializable;

/** API 资源 */
@Data
public class ApiResource implements Serializable {
    private String apiCode;
    private String apiMethod;
    private String apiUri;
    private String apiName;
    private String module;
    private String appCode;
    private Boolean writeFlag;
}
```

`DataScope.java`：

```java
package com.wkclz.auth.model;

import lombok.Data;
import java.io.Serializable;

/** 数据权限范围 */
@Data
public class DataScope implements Serializable {
    private String dimensionCode;
    private String dimensionName;
    private String scopeValue;
    private String appCode;
}
```

`FieldPermission.java`：

```java
package com.wkclz.auth.model;

import com.wkclz.auth.enums.FieldPermissionType;
import lombok.Data;
import java.io.Serializable;

/** 字段权限 */
@Data
public class FieldPermission implements Serializable {
    private String fieldName;
    private FieldPermissionType type;
    private String apiCode;
}
```

- [ ] **步骤 2：验证编译**

```bash
mvn compile -pl sh-auth -am
```

预期：BUILD SUCCESS

- [ ] **步骤 3：Commit**

```bash
git add sh-auth/src/main/java/com/wkclz/auth/model/Role.java sh-auth/src/main/java/com/wkclz/auth/model/MenuNode.java sh-auth/src/main/java/com/wkclz/auth/model/ApiResource.java sh-auth/src/main/java/com/wkclz/auth/model/DataScope.java sh-auth/src/main/java/com/wkclz/auth/model/FieldPermission.java
git commit -m "feat: add RBAC resource models (Role, MenuNode, ApiResource, DataScope, FieldPermission)"
```

---

### 任务 8：模型 — 关联关系（3 个）+ 日志/基础设施模型

**文件：**
- 创建：`sh-auth/src/main/java/com/wkclz/auth/model/SubjectRole.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/model/RoleDataScope.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/model/ApiField.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/model/LoginRecord.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/model/RequestRecord.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/model/SecurityHeaders.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/model/AuthMetadata.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/model/SubjectAuthorization.java`

- [ ] **步骤 1：编写全部 3 个关联关系模型**

`SubjectRole.java`：

```java
package com.wkclz.auth.model;

import lombok.Data;
import java.io.Serializable;
import java.time.LocalDateTime;

/** 用户-角色关联 */
@Data
public class SubjectRole implements Serializable {
    private String subjectId;
    private String roleCode;
    private LocalDateTime startTime;
    private LocalDateTime endTime;
    private Boolean enableStatus;
}
```

`RoleDataScope.java`：

```java
package com.wkclz.auth.model;

import lombok.Data;
import java.io.Serializable;

/** 角色-数据权限关联 */
@Data
public class RoleDataScope implements Serializable {
    private String roleCode;
    private String dimensionCode;
    private String scopeValue;
}
```

`ApiField.java`：

```java
package com.wkclz.auth.model;

import com.wkclz.auth.enums.FieldPermissionType;
import lombok.Data;
import java.io.Serializable;

/** API-字段权限关联 */
@Data
public class ApiField implements Serializable {
    private String apiCode;
    private String fieldName;
    private FieldPermissionType type;
}
```

`LoginRecord.java`：

```java
package com.wkclz.auth.model;

import lombok.Data;
import java.io.Serializable;
import java.time.LocalDateTime;

/** 登录日志 */
@Data
public class LoginRecord implements Serializable {
    private String id;
    private String subjectId;
    private String username;
    private String authType;
    private String clientIp;
    private String userAgent;
    private Boolean success;
    private String failReason;
    private LocalDateTime loginTime;
}
```

`RequestRecord.java`：

```java
package com.wkclz.auth.model;

import lombok.Data;
import java.io.Serializable;
import java.time.LocalDateTime;

/** 请求日志 */
@Data
public class RequestRecord implements Serializable {
    private String id;
    private String requestUri;
    private String requestMethod;
    private String userId;
    private String username;
    private String clientIp;
    private String userAgent;
    private String requestBody;
    private Integer responseStatus;
    private String responseBody;
    private Long costTime;
    private LocalDateTime requestTime;
    private String authType;
    private String exception;
}
```

`SecurityHeaders.java`：

```java
package com.wkclz.auth.model;

import lombok.Data;
import java.io.Serializable;

/** HTTP 安全头 */
@Data
public class SecurityHeaders implements Serializable {
    private String contentSecurityPolicy;
    private String xFrameOptions;
    private String xContentTypeOptions;
    private String strictTransportSecurity;
    private String xXssProtection;
    private String referrerPolicy;
}
```

- [ ] **步骤 2：验证编译**

```bash
mvn compile -pl sh-auth -am
```

预期：BUILD SUCCESS

- [ ] **步骤 3：Commit**

```bash
git add sh-auth/src/main/java/com/wkclz/auth/model/SubjectRole.java sh-auth/src/main/java/com/wkclz/auth/model/RoleDataScope.java sh-auth/src/main/java/com/wkclz/auth/model/ApiField.java sh-auth/src/main/java/com/wkclz/auth/model/LoginRecord.java sh-auth/src/main/java/com/wkclz/auth/model/RequestRecord.java sh-auth/src/main/java/com/wkclz/auth/model/SecurityHeaders.java sh-auth/src/main/java/com/wkclz/auth/model/AuthMetadata.java sh-auth/src/main/java/com/wkclz/auth/model/SubjectAuthorization.java
git commit -m "feat: add relation + log + infra models (3 relations + 3 infra + 2 cache snapshots)"
```

---

### 任务 9：SecurityContext

**文件：**
- 创建：`sh-auth/src/main/java/com/wkclz/auth/context/SecurityContext.java`

- [ ] **步骤 1：编写 SecurityContext**

```java
package com.wkclz.auth.context;

import com.wkclz.auth.model.Principal;

/**
 * 请求级安全上下文（ThreadLocal）
 */
public class SecurityContext {

    private static final ThreadLocal<Principal> PRINCIPAL_HOLDER = new ThreadLocal<>();
    private static final ThreadLocal<String> TOKEN_HOLDER = new ThreadLocal<>();
    private static final ThreadLocal<String> TENANT_CODE_HOLDER = new ThreadLocal<>();
    private static final ThreadLocal<String> APP_CODE_HOLDER = new ThreadLocal<>();

    public static Principal getPrincipal() {
        return PRINCIPAL_HOLDER.get();
    }

    public static void setPrincipal(Principal principal) {
        PRINCIPAL_HOLDER.set(principal);
    }

    public static String getToken() {
        return TOKEN_HOLDER.get();
    }

    public static void setToken(String token) {
        TOKEN_HOLDER.set(token);
    }

    public static String getUserCode() {
        Principal p = getPrincipal();
        return p != null ? p.getUserCode() : null;
    }

    public static String getUsername() {
        Principal p = getPrincipal();
        return p != null ? p.getUsername() : null;
    }

    public static String getTenantCode() {
        return TENANT_CODE_HOLDER.get();
    }

    public static void setTenantCode(String tenantCode) {
        TENANT_CODE_HOLDER.set(tenantCode);
    }

    public static String getAppCode() {
        return APP_CODE_HOLDER.get();
    }

    public static void setAppCode(String appCode) {
        APP_CODE_HOLDER.set(appCode);
    }

    /** 请求结束清理，防止内存泄漏 */
    public static void clear() {
        PRINCIPAL_HOLDER.remove();
        TOKEN_HOLDER.remove();
        TENANT_CODE_HOLDER.remove();
        APP_CODE_HOLDER.remove();
    }
}
```

- [ ] **步骤 2：验证编译**

```bash
mvn compile -pl sh-auth -am
```

预期：BUILD SUCCESS

- [ ] **步骤 3：Commit**

```bash
git add sh-auth/src/main/java/com/wkclz/auth/context/SecurityContext.java
git commit -m "feat: add SecurityContext (ThreadLocal-based security context)"
```

---

### 任务 10：SPI — 认证域接口（第 1 批）

**文件：**
- 创建：`sh-auth/src/main/java/com/wkclz/auth/contract/auth/AuthenticationProvider.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/contract/auth/TokenService.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/contract/auth/PasswordEncoder.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/contract/auth/PasswordValidator.java`

- [ ] **步骤 1：编写 AuthenticationProvider**

```java
package com.wkclz.auth.contract.auth;

import com.wkclz.auth.model.AuthRequest;
import com.wkclz.auth.model.AuthResult;
import jakarta.servlet.http.HttpServletRequest;
import java.util.List;

/**
 * 认证提供者 SPI（核心扩展点）
 */
public interface AuthenticationProvider {

    AuthResult authenticate(AuthRequest request, HttpServletRequest httpRequest);

    List<String> supportedAuthTypes();

    default int getOrder() {
        return 0;
    }
}
```

- [ ] **步骤 2：编写 TokenService**

```java
package com.wkclz.auth.contract.auth;

import com.wkclz.auth.enums.TokenType;
import com.wkclz.auth.model.AuthToken;
import com.wkclz.auth.model.Principal;

/**
 * Token 生命周期管理
 */
public interface TokenService {

    AuthToken generateToken(Principal principal);

    Principal parseToken(String tokenValue);

    boolean validateToken(String tokenValue);

    AuthToken refreshToken(String tokenValue);

    TokenType getTokenType();
}
```

- [ ] **步骤 3：编写 PasswordEncoder**

```java
package com.wkclz.auth.contract.auth;

/**
 * 密码编码器
 */
public interface PasswordEncoder {

    String encode(String rawPassword);

    boolean matches(String rawPassword, String encodedPassword);
}
```

- [ ] **步骤 4：编写 PasswordValidator**

```java
package com.wkclz.auth.contract.auth;

/**
 * 密码策略校验器
 */
public interface PasswordValidator {

    void validateStrength(String password);

    boolean isHistoryPassword(String subjectId, String password);

    boolean isExpired(String subjectId);

    long getRemainingDays(String subjectId);
}
```

- [ ] **步骤 5：验证编译**

```bash
mvn compile -pl sh-auth -am
```

预期：BUILD SUCCESS

- [ ] **步骤 6：Commit**

```bash
git add sh-auth/src/main/java/com/wkclz/auth/contract/auth/AuthenticationProvider.java sh-auth/src/main/java/com/wkclz/auth/contract/auth/TokenService.java sh-auth/src/main/java/com/wkclz/auth/contract/auth/PasswordEncoder.java sh-auth/src/main/java/com/wkclz/auth/contract/auth/PasswordValidator.java
git commit -m "feat: add auth SPI batch 1 (AuthenticationProvider, TokenService, PasswordEncoder, PasswordValidator)"
```

---

### 任务 11：SPI — 认证域接口（第 2 批）

**文件：**
- 创建：`sh-auth/src/main/java/com/wkclz/auth/contract/auth/CredentialProvider.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/contract/auth/SmsCodeSender.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/contract/auth/OAuthProvider.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/contract/auth/CaptchaService.java`

- [ ] **步骤 1：编写 CredentialProvider**

```java
package com.wkclz.auth.contract.auth;

import com.wkclz.auth.enums.CredentialType;
import com.wkclz.auth.model.Credential;

/**
 * 凭证提供者 SPI
 */
public interface CredentialProvider {

    CredentialType supportedType();

    String verify(Credential credential);
}
```

- [ ] **步骤 2：编写 SmsCodeSender**

```java
package com.wkclz.auth.contract.auth;

/**
 * 短信验证码发送
 */
public interface SmsCodeSender {

    boolean send(String phoneNumber, String code);
}
```

- [ ] **步骤 3：编写 OAuthProvider**

```java
package com.wkclz.auth.contract.auth;

import com.wkclz.auth.model.AuthToken;
import com.wkclz.auth.model.Principal;

/**
 * 第三方 OAuth 接入 SPI
 */
public interface OAuthProvider {

    String getProviderName();

    String getAuthorizationUrl(String redirectUri, String state);

    Principal getUserInfo(String code, String state);

    AuthToken getAccessToken(String code, String state);
}
```

- [ ] **步骤 4：编写 CaptchaService**

```java
package com.wkclz.auth.contract.auth;

import com.wkclz.auth.model.Captcha;

/**
 * 图形验证码服务
 */
public interface CaptchaService {

    Captcha generate();

    boolean verify(String captchaId, String captchaCode);
}
```

- [ ] **步骤 5：验证编译**

```bash
mvn compile -pl sh-auth -am
```

预期：BUILD SUCCESS

- [ ] **步骤 6：Commit**

```bash
git add sh-auth/src/main/java/com/wkclz/auth/contract/auth/CredentialProvider.java sh-auth/src/main/java/com/wkclz/auth/contract/auth/SmsCodeSender.java sh-auth/src/main/java/com/wkclz/auth/contract/auth/OAuthProvider.java sh-auth/src/main/java/com/wkclz/auth/contract/auth/CaptchaService.java
git commit -m "feat: add auth SPI batch 2 (CredentialProvider, SmsCodeSender, OAuthProvider, CaptchaService)"
```

---

### 任务 12：SPI — 认证域接口（第 3 批）+ 登出 + 授权域 + 基础设施

**文件：**
- 创建：`sh-auth/src/main/java/com/wkclz/auth/contract/auth/MfaService.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/contract/auth/AccountStatusChecker.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/contract/auth/SessionStore.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/contract/auth/ConcurrentSessionControl.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/contract/auth/RateLimitChecker.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/contract/auth/LogoutService.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/contract/auth/DefaultLogoutService.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/contract/authz/AccessControlProvider.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/contract/authz/MenuProvider.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/contract/authz/DataScopeProvider.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/contract/authz/FieldPermissionProvider.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/contract/infra/RequestLogger.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/contract/infra/SecurityHeaderProvider.java`

- [ ] **步骤 1：编写 MfaService**

```java
package com.wkclz.auth.contract.auth;

import com.wkclz.auth.enums.MfaType;
import com.wkclz.auth.model.MfaChallenge;

/**
 * MFA 服务
 */
public interface MfaService {

    boolean isMfaRequired(String subjectId);

    MfaChallenge sendChallenge(String subjectId, MfaType type);

    boolean verifyChallenge(String challengeId, String code);
}
```

- [ ] **步骤 2：编写 AccountStatusChecker**

```java
package com.wkclz.auth.contract.auth;

import com.wkclz.auth.exception.AccountStatusException;

/**
 * 账号状态检查
 */
public interface AccountStatusChecker {

    void checkStatus(String subjectId) throws AccountStatusException;
}
```

- [ ] **步骤 3：编写 SessionStore**

```java
package com.wkclz.auth.contract.auth;

import com.wkclz.auth.model.Session;
import java.util.List;

/**
 * 会话存储
 */
public interface SessionStore {

    void save(Session session);

    Session get(String sessionId);

    void delete(String sessionId);

    void deleteBySubjectId(String subjectId);

    void refresh(String sessionId, long ttlSeconds);

    List<Session> getActiveSessions(String subjectId);
}
```

- [ ] **步骤 4：编写 ConcurrentSessionControl**

```java
package com.wkclz.auth.contract.auth;

/**
 * 并发会话控制
 */
public interface ConcurrentSessionControl {

    void enforce(String subjectId);

    int getCurrentCount(String subjectId);

    int getMaxSessions();
}
```

- [ ] **步骤 5：编写 RateLimitChecker**

```java
package com.wkclz.auth.contract.auth;

/**
 * 登录频率限制
 */
public interface RateLimitChecker {

    void check(String identifier);

    void recordAttempt(String identifier, boolean success);

    void reset(String identifier);
}
```

- [ ] **步骤 6：编写 LogoutService + DefaultLogoutService**

```java
package com.wkclz.auth.contract.auth;

/**
 * 登出服务
 */
public interface LogoutService {

    void logout();

    void logout(String sessionId);

    void invalidateAllSessions(String subjectId);
}
```

```java
package com.wkclz.auth.contract.auth;

import com.wkclz.auth.context.SecurityContext;
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;

/**
 * 基于 SessionStore 的默认登出实现
 */
@Slf4j
@RequiredArgsConstructor
public class DefaultLogoutService implements LogoutService {

    private final SessionStore sessionStore;

    @Override
    public void logout() {
        String token = SecurityContext.getToken();
        if (token != null) {
            sessionStore.delete(token);
            log.debug("已登出当前会话: {}", token);
        }
    }

    @Override
    public void logout(String sessionId) {
        sessionStore.delete(sessionId);
        log.debug("已踢出会话: {}", sessionId);
    }

    @Override
    public void invalidateAllSessions(String subjectId) {
        sessionStore.deleteBySubjectId(subjectId);
        log.info("已使所有会话失效: subjectId={}", subjectId);
    }
}
```

- [ ] **步骤 7：编写授权域 4 个 SPI**

`AccessControlProvider.java`：

```java
package com.wkclz.auth.contract.authz;

import com.wkclz.auth.model.AuthPermission;
import com.wkclz.auth.model.Principal;
import java.util.List;

/**
 * API 级访问控制
 */
public interface AccessControlProvider {

    boolean hasPermission(Principal principal, String apiMethod, String apiUri);

    List<AuthPermission> getUserPermissions(Principal principal);
}
```

`MenuProvider.java`：

```java
package com.wkclz.auth.contract.authz;

import com.wkclz.auth.model.MenuNode;
import com.wkclz.auth.model.Principal;
import java.util.List;

/**
 * 菜单/资源树提供者
 */
public interface MenuProvider {

    List<MenuNode> getUserMenuTree(Principal principal);

    List<String> getUserButtonPermissions(Principal principal);
}
```

`DataScopeProvider.java`：

```java
package com.wkclz.auth.contract.authz;

import com.wkclz.auth.model.DataScope;
import com.wkclz.auth.model.Principal;
import java.util.List;

/**
 * 数据权限范围提供者
 */
public interface DataScopeProvider {

    List<DataScope> getDataScopes(Principal principal);

    List<DataScope> getDataScopesByDimension(Principal principal, String dimensionCode);
}
```

`FieldPermissionProvider.java`：

```java
package com.wkclz.auth.contract.authz;

import com.wkclz.auth.model.FieldPermission;
import com.wkclz.auth.model.Principal;
import java.util.List;

/**
 * 字段级权限提供者
 */
public interface FieldPermissionProvider {

    List<FieldPermission> getFieldPermissions(String apiCode);

    List<FieldPermission> getUserFieldPermissions(Principal principal, String apiCode);
}
```

- [ ] **步骤 8：编写基础设施 2 个 SPI**

`RequestLogger.java`：

```java
package com.wkclz.auth.contract.infra;

import com.wkclz.auth.model.RequestRecord;

/**
 * 请求日志记录器
 */
public interface RequestLogger {

    void save(RequestRecord record);
}
```

`SecurityHeaderProvider.java`：

```java
package com.wkclz.auth.contract.infra;

import com.wkclz.auth.model.SecurityHeaders;

/**
 * HTTP 安全头提供者
 */
public interface SecurityHeaderProvider {

    SecurityHeaders getHeaders();
}
```

- [ ] **步骤 9：验证编译**

```bash
mvn compile -pl sh-auth -am
```

预期：BUILD SUCCESS

- [ ] **步骤 10：Commit**

```bash
git add sh-auth/src/main/java/com/wkclz/auth/contract/
git commit -m "feat: add remaining SPI interfaces (auth batch 3, authz 4, infra 2, LogoutService + DefaultLogoutService)"
```

---

### 任务 13：LoginService 模板方法

**文件：**
- 创建：`sh-auth/src/main/java/com/wkclz/auth/contract/auth/LoginService.java`

- [ ] **步骤 1：编写 LoginService**

```java
package com.wkclz.auth.contract.auth;

import com.wkclz.auth.enums.AuthErrorType;
import com.wkclz.auth.enums.AuthStatus;
import com.wkclz.auth.exception.AccountStatusException;
import com.wkclz.auth.exception.AuthenticationException;
import com.wkclz.auth.exception.RateLimitException;
import com.wkclz.auth.model.*;
import jakarta.servlet.http.HttpServletRequest;
import lombok.extern.slf4j.Slf4j;

/**
 * 登录编排模板方法
 */
@Slf4j
public abstract class LoginService {

    protected AuthenticationProvider authProvider;
    protected TokenService tokenService;
    protected SessionStore sessionStore;
    protected RateLimitChecker rateLimitChecker;
    protected CaptchaService captchaService;
    protected AccountStatusChecker accountStatusChecker;
    protected MfaService mfaService;
    protected ConcurrentSessionControl concurrentSessionControl;

    /**
     * 模板方法：统一登录流程
     */
    public AuthResult login(AuthRequest request, HttpServletRequest httpRequest) {
        String identifier = resolveIdentifier(request, httpRequest);
        try {
            // 1. 登录频率检查
            checkRateLimit(request, httpRequest);

            // 2. 图形验证码校验
            checkCaptcha(request);

            // 3. 调用 AuthenticationProvider 执行认证
            AuthResult result = authProvider.authenticate(request, httpRequest);
            if (!result.isSuccess()) {
                rateLimitChecker.recordAttempt(identifier, false);
                recordLoginLog(result, httpRequest);
                return result;
            }

            // 4. 账号状态检查
            accountStatusChecker.checkStatus(result.getPrincipal().getUserCode());

            // 5. MFA 二次验证（如需要）
            MfaChallenge challenge = checkMfa(result.getPrincipal());
            if (challenge != null) {
                result.setStatus(AuthStatus.MFA_REQUIRED);
                result.setMfaChallenge(challenge);
                return result;
            }

            // 6. 生成 Token 并创建会话
            AuthToken token = tokenService.generateToken(result.getPrincipal());
            result.setToken(token);
            result.setStatus(AuthStatus.SUCCESS);

            Session session = createSession(result.getPrincipal(), token, httpRequest);
            concurrentSessionControl.enforce(session.getSubjectId());
            sessionStore.save(session);

            // 7. 记录登录日志
            rateLimitChecker.recordAttempt(identifier, true);
            recordLoginLog(result, httpRequest);

            return result;
        } catch (AuthenticationException e) {
            log.warn("认证失败: {}", e.getMessage());
            rateLimitChecker.recordAttempt(identifier, false);
            return AuthResult.fail(AuthErrorType.BAD_CREDENTIALS, e.getMessage());
        } catch (RateLimitException e) {
            log.warn("登录频率超限: {}", e.getMessage());
            return AuthResult.fail(AuthErrorType.RATE_LIMITED, e.getMessage());
        } catch (AccountStatusException e) {
            log.warn("账号状态异常: {}", e.getMessage());
            return AuthResult.fail(AuthErrorType.ACCOUNT_DISABLED, e.getMessage());
        } catch (Exception e) {
            log.error("登录异常", e);
            return AuthResult.fail(AuthErrorType.INTERNAL_ERROR, e.getMessage());
        }
    }

    /** 解析限流标识（默认取 IP，子类可覆盖为用户名） */
    protected String resolveIdentifier(AuthRequest request, HttpServletRequest httpRequest) {
        return httpRequest.getRemoteAddr();
    }

    /** 子步骤，由接入方实现 */
    protected abstract void checkRateLimit(AuthRequest request, HttpServletRequest httpRequest);
    protected abstract void checkCaptcha(AuthRequest request);
    protected abstract MfaChallenge checkMfa(Principal principal);
    protected abstract Session createSession(Principal principal, AuthToken token, HttpServletRequest httpRequest);
    protected abstract void recordLoginLog(AuthResult result, HttpServletRequest httpRequest);
}
```

- [ ] **步骤 2：验证编译**

```bash
mvn compile -pl sh-auth -am
```

预期：BUILD SUCCESS

- [ ] **步骤 3：Commit**

```bash
git add sh-auth/src/main/java/com/wkclz/auth/contract/auth/LoginService.java
git commit -m "feat: add LoginService template method with complete login flow"
```

---

### 任务 14：过滤器 — RequestWrapperFilter + EagerContentCachingRequestWrapper

**文件：**
- 创建：`sh-auth/src/main/java/com/wkclz/auth/filter/EagerContentCachingRequestWrapper.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/filter/RequestWrapperFilter.java`

- [ ] **步骤 1：编写 EagerContentCachingRequestWrapper**

```java
package com.wkclz.auth.filter;

import jakarta.servlet.ReadListener;
import jakarta.servlet.ServletInputStream;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletRequestWrapper;
import org.springframework.util.StreamUtils;

import java.io.BufferedReader;
import java.io.ByteArrayInputStream;
import java.io.IOException;
import java.io.InputStreamReader;

/**
 * 主动缓存请求体的 ContentCachingRequestWrapper
 */
public class EagerContentCachingRequestWrapper extends HttpServletRequestWrapper {

    private byte[] cachedBody;

    public EagerContentCachingRequestWrapper(HttpServletRequest request) throws IOException {
        super(request);
        this.cachedBody = StreamUtils.copyToByteArray(request.getInputStream());
    }

    @Override
    public ServletInputStream getInputStream() {
        ByteArrayInputStream bais = new ByteArrayInputStream(cachedBody);
        return new ServletInputStream() {
            @Override
            public int read() {
                return bais.read();
            }

            @Override
            public boolean isFinished() {
                return bais.available() == 0;
            }

            @Override
            public boolean isReady() {
                return true;
            }

            @Override
            public void setReadListener(ReadListener listener) {
            }
        };
    }

    @Override
    public BufferedReader getReader() {
        return new BufferedReader(new InputStreamReader(getInputStream()));
    }

    public byte[] getCachedBody() {
        return cachedBody;
    }
}
```

- [ ] **步骤 2：编写 RequestWrapperFilter**

```java
package com.wkclz.auth.filter;

import jakarta.servlet.FilterChain;
import jakarta.servlet.ServletException;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;
import lombok.extern.slf4j.Slf4j;
import org.springframework.core.Ordered;
import org.springframework.web.filter.OncePerRequestFilter;

import java.io.IOException;

/**
 * 请求体可重复读取包装过滤器（最高优先级）
 */
@Slf4j
public class RequestWrapperFilter extends OncePerRequestFilter implements Ordered {

    @Override
    public int getOrder() {
        return Integer.MIN_VALUE;
    }

    @Override
    protected void doFilterInternal(HttpServletRequest request, HttpServletResponse response,
                                    FilterChain filterChain) throws ServletException, IOException {
        EagerContentCachingRequestWrapper wrappedRequest = new EagerContentCachingRequestWrapper(request);
        filterChain.doFilter(wrappedRequest, response);
    }
}
```

- [ ] **步骤 3：验证编译**

```bash
mvn compile -pl sh-auth -am
```

预期：BUILD SUCCESS

- [ ] **步骤 4：Commit**

```bash
git add sh-auth/src/main/java/com/wkclz/auth/filter/EagerContentCachingRequestWrapper.java sh-auth/src/main/java/com/wkclz/auth/filter/RequestWrapperFilter.java
git commit -m "feat: add RequestWrapperFilter + EagerContentCachingRequestWrapper"
```

---

### 任务 15：过滤器 — SecurityHeaderFilter + AuthenticationFilter + AuthorizationFilter

**文件：**
- 创建：`sh-auth/src/main/java/com/wkclz/auth/filter/SecurityHeaderFilter.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/filter/AuthenticationFilter.java`
- 创建：`sh-auth/src/main/java/com/wkclz/auth/filter/AuthorizationFilter.java`

- [ ] **步骤 1：编写 SecurityHeaderFilter**

```java
package com.wkclz.auth.filter;

import com.wkclz.auth.contract.infra.SecurityHeaderProvider;
import com.wkclz.auth.model.SecurityHeaders;
import jakarta.servlet.FilterChain;
import jakarta.servlet.ServletException;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.web.filter.OncePerRequestFilter;

import java.io.IOException;
import java.util.List;

/**
 * HTTP 安全头注入过滤器
 */
@Slf4j
@RequiredArgsConstructor
public class SecurityHeaderFilter extends OncePerRequestFilter {

    private final List<SecurityHeaderProvider> headerProviders;

    @Override
    protected void doFilterInternal(HttpServletRequest request, HttpServletResponse response,
                                    FilterChain filterChain) throws ServletException, IOException {
        for (SecurityHeaderProvider provider : headerProviders) {
            SecurityHeaders headers = provider.getHeaders();
            if (headers == null) continue;
            setIfNotNull(response, "X-Frame-Options", headers.getXFrameOptions());
            setIfNotNull(response, "X-Content-Type-Options", headers.getXContentTypeOptions());
            setIfNotNull(response, "Strict-Transport-Security", headers.getStrictTransportSecurity());
            setIfNotNull(response, "X-XSS-Protection", headers.getXXssProtection());
            setIfNotNull(response, "Referrer-Policy", headers.getReferrerPolicy());
            setIfNotNull(response, "Content-Security-Policy", headers.getContentSecurityPolicy());
        }
        filterChain.doFilter(request, response);
    }

    private void setIfNotNull(HttpServletResponse response, String name, String value) {
        if (value != null && !value.isEmpty()) {
            response.setHeader(name, value);
        }
    }
}
```

- [ ] **步骤 2：编写 AuthenticationFilter**

```java
package com.wkclz.auth.filter;

import com.wkclz.auth.config.AuthConstants;
import com.wkclz.auth.config.AuthProperties;
import com.wkclz.auth.contract.auth.SessionStore;
import com.wkclz.auth.contract.auth.TokenService;
import com.wkclz.auth.context.SecurityContext;
import com.wkclz.auth.exception.AuthenticationException;
import com.wkclz.auth.exception.SessionExpiredException;
import com.wkclz.auth.model.Principal;
import com.wkclz.auth.model.Session;
import jakarta.servlet.FilterChain;
import jakarta.servlet.ServletException;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.util.AntPathMatcher;
import org.springframework.web.filter.OncePerRequestFilter;

import java.io.IOException;

/**
 * 认证过滤器
 */
@Slf4j
@RequiredArgsConstructor
public class AuthenticationFilter extends OncePerRequestFilter {

    private final TokenService tokenService;
    private final SessionStore sessionStore;
    private final AuthProperties properties;

    private static final AntPathMatcher PATH_MATCHER = new AntPathMatcher();

    @Override
    protected void doFilterInternal(HttpServletRequest request, HttpServletResponse response,
                                    FilterChain filterChain) throws ServletException, IOException {
        // 白名单放行
        if (isWhiteListed(request.getRequestURI())) {
            filterChain.doFilter(request, response);
            return;
        }

        try {
            // 获取 Token
            String token = resolveToken(request);
            if (token == null || token.isEmpty()) {
                sendError(response, HttpServletResponse.SC_UNAUTHORIZED, "缺少认证 Token");
                return;
            }

            // 解析 Token
            Principal principal = tokenService.parseToken(token);
            if (principal == null) {
                sendError(response, HttpServletResponse.SC_UNAUTHORIZED, "Token 解析失败");
                return;
            }

            // 验证 Token
            if (!tokenService.validateToken(token)) {
                sendError(response, HttpServletResponse.SC_UNAUTHORIZED, "Token 无效或已过期");
                return;
            }

            // 检查会话
            Session session = sessionStore.get(token);
            if (session == null) {
                sendError(response, HttpServletResponse.SC_UNAUTHORIZED, "会话不存在或已过期");
                return;
            }

            // 设置安全上下文
            SecurityContext.setPrincipal(principal);
            SecurityContext.setToken(token);

            filterChain.doFilter(request, response);
        } catch (AuthenticationException e) {
            log.warn("认证失败: {}", e.getMessage());
            sendError(response, HttpServletResponse.SC_UNAUTHORIZED, e.getMessage());
        } catch (SessionExpiredException e) {
            log.warn("会话过期: {}", e.getMessage());
            sendError(response, HttpServletResponse.SC_UNAUTHORIZED, e.getMessage());
        }
    }

    private String resolveToken(HttpServletRequest request) {
        // 优先从 Authorization 头获取
        String bearer = request.getHeader(AuthConstants.DEFAULT_TOKEN_HEADER);
        if (bearer != null && bearer.startsWith(AuthConstants.BEARER_PREFIX)) {
            return bearer.substring(AuthConstants.BEARER_PREFIX.length());
        }
        // 其次从自定义 token 头获取
        return request.getHeader(AuthConstants.CUSTOM_TOKEN_HEADER);
    }

    private boolean isWhiteListed(String uri) {
        return properties.getWhiteList().getPaths().stream()
                .anyMatch(pattern -> PATH_MATCHER.match(pattern, uri));
    }

    private void sendError(HttpServletResponse response, int status, String message) throws IOException {
        response.setStatus(status);
        response.setContentType("application/json;charset=UTF-8");
        response.getWriter().write("{\"code\":" + status + ",\"message\":\"" + message + "\"}");
    }
}
```

- [ ] **步骤 3：编写 AuthorizationFilter**

```java
package com.wkclz.auth.filter;

import com.wkclz.auth.config.AuthProperties;
import com.wkclz.auth.contract.authz.AccessControlProvider;
import com.wkclz.auth.context.SecurityContext;
import com.wkclz.auth.model.Principal;
import jakarta.servlet.FilterChain;
import jakarta.servlet.ServletException;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.util.AntPathMatcher;
import org.springframework.web.filter.OncePerRequestFilter;

import java.io.IOException;
import java.util.List;

/**
 * 鉴权过滤器
 */
@Slf4j
@RequiredArgsConstructor
public class AuthorizationFilter extends OncePerRequestFilter {

    private final List<AccessControlProvider> accessControlProviders;
    private final AuthProperties properties;

    private static final AntPathMatcher PATH_MATCHER = new AntPathMatcher();

    @Override
    protected void doFilterInternal(HttpServletRequest request, HttpServletResponse response,
                                    FilterChain filterChain) throws ServletException, IOException {
        // 白名单放行
        if (isWhiteListed(request.getRequestURI())) {
            filterChain.doFilter(request, response);
            return;
        }

        // 无 AccessControlProvider 实现时放行（仅记录日志）
        if (accessControlProviders == null || accessControlProviders.isEmpty()) {
            log.debug("无 AccessControlProvider 实现，鉴权放行: {}", request.getRequestURI());
            filterChain.doFilter(request, response);
            return;
        }

        Principal principal = SecurityContext.getPrincipal();
        if (principal == null) {
            filterChain.doFilter(request, response);
            return;
        }

        // 任一拒绝则拒绝
        for (AccessControlProvider provider : accessControlProviders) {
            if (!provider.hasPermission(principal, request.getMethod(), request.getRequestURI())) {
                log.warn("鉴权拒绝: user={}, method={}, uri={}", principal.getUsername(),
                        request.getMethod(), request.getRequestURI());
                sendError(response, HttpServletResponse.SC_FORBIDDEN, "无权访问");
                return;
            }
        }

        filterChain.doFilter(request, response);
    }

    private boolean isWhiteListed(String uri) {
        return properties.getWhiteList().getPaths().stream()
                .anyMatch(pattern -> PATH_MATCHER.match(pattern, uri));
    }

    private void sendError(HttpServletResponse response, int status, String message) throws IOException {
        response.setStatus(status);
        response.setContentType("application/json;charset=UTF-8");
        response.getWriter().write("{\"code\":" + status + ",\"message\":\"" + message + "\"}");
    }
}
```

- [ ] **步骤 4：验证编译**

```bash
mvn compile -pl sh-auth -am
```

预期：BUILD SUCCESS

- [ ] **步骤 5：Commit**

```bash
git add sh-auth/src/main/java/com/wkclz/auth/filter/SecurityHeaderFilter.java sh-auth/src/main/java/com/wkclz/auth/filter/AuthenticationFilter.java sh-auth/src/main/java/com/wkclz/auth/filter/AuthorizationFilter.java
git commit -m "feat: add SecurityHeaderFilter, AuthenticationFilter, AuthorizationFilter"
```

---

### 任务 16：过滤器 — RequestLogFilter

**文件：**
- 创建：`sh-auth/src/main/java/com/wkclz/auth/filter/RequestLogFilter.java`

- [ ] **步骤 1：编写 RequestLogFilter**

```java
package com.wkclz.auth.filter;

import com.wkclz.auth.contract.infra.RequestLogger;
import com.wkclz.auth.context.SecurityContext;
import com.wkclz.auth.model.RequestRecord;
import jakarta.servlet.FilterChain;
import jakarta.servlet.ServletException;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.web.filter.OncePerRequestFilter;
import org.springframework.web.util.ContentCachingResponseWrapper;

import java.io.IOException;
import java.time.LocalDateTime;
import java.util.List;

/**
 * 请求日志过滤器（最外层，确保任何请求都记录）
 */
@Slf4j
@RequiredArgsConstructor
public class RequestLogFilter extends OncePerRequestFilter {

    private final List<RequestLogger> requestLoggers;

    @Override
    protected void doFilterInternal(HttpServletRequest request, HttpServletResponse response,
                                    FilterChain filterChain) throws ServletException, IOException {
        long startTime = System.currentTimeMillis();
        ContentCachingResponseWrapper responseWrapper = new ContentCachingResponseWrapper(response);

        try {
            filterChain.doFilter(request, responseWrapper);
        } finally {
            try {
                // 采集日志
                RequestRecord record = buildRecord(request, responseWrapper, startTime);
                // 异步保存
                for (RequestLogger logger : requestLoggers) {
                    try {
                        logger.save(record);
                    } catch (Exception e) {
                        log.error("保存请求日志失败", e);
                    }
                }
                // 清理 SecurityContext
                SecurityContext.clear();
            } catch (Exception e) {
                log.error("请求日志采集异常", e);
            } finally {
                responseWrapper.copyBodyToResponse();
            }
        }
    }

    private RequestRecord buildRecord(HttpServletRequest request, ContentCachingResponseWrapper response,
                                       long startTime) {
        RequestRecord record = new RequestRecord();
        record.setRequestUri(request.getRequestURI());
        record.setRequestMethod(request.getMethod());
        record.setClientIp(request.getRemoteAddr());
        record.setUserAgent(request.getHeader("User-Agent"));
        record.setResponseStatus(response.getStatus());
        record.setCostTime(System.currentTimeMillis() - startTime);
        record.setRequestTime(LocalDateTime.now());

        com.wkclz.auth.model.Principal principal = SecurityContext.getPrincipal();
        if (principal != null) {
            record.setUserId(principal.getUserCode());
            record.setUsername(principal.getUsername());
        }

        // 请求体截断
        if (request instanceof EagerContentCachingRequestWrapper wrapper) {
            byte[] body = wrapper.getCachedBody();
            if (body != null && body.length > 0) {
                record.setRequestBody(maskSensitive(new String(body), 4096));
            }
        }

        // 响应体截断
        byte[] respBody = response.getContentAsByteArray();
        if (respBody != null && respBody.length > 0) {
            record.setResponseBody(truncate(new String(respBody), 4096));
        }

        return record;
    }

    private String maskSensitive(String body, int maxLen) {
        // password 字段脱敏
        String masked = body.replaceAll("\"password\"\\s*:\\s*\"[^\"]*\"", "\"password\":\"******\"");
        return truncate(masked, maxLen);
    }

    private String truncate(String str, int maxLen) {
        if (str == null) return null;
        return str.length() > maxLen ? str.substring(0, maxLen) + "...(truncated)" : str;
    }
}
```

- [ ] **步骤 2：验证编译**

```bash
mvn compile -pl sh-auth -am
```

预期：BUILD SUCCESS

- [ ] **步骤 3：Commit**

```bash
git add sh-auth/src/main/java/com/wkclz/auth/filter/RequestLogFilter.java
git commit -m "feat: add RequestLogFilter with SecurityContext.clear() in finally"
```

---

### 任务 17：ShAuthAutoConfiguration + FilterRegistrationBean + AutoConfiguration.imports

**文件：**
- 创建：`sh-auth/src/main/java/com/wkclz/auth/ShAuthAutoConfiguration.java`
- 创建：`sh-auth/src/main/resources/META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports`

- [ ] **步骤 1：编写 FilterOrder 常量（直接在 AutoConfiguration 类中定义）**

先在 `filter` 包创建一个单独常量类避免循环依赖：

```java
package com.wkclz.auth.filter;

/**
 * Filter 排序常量
 */
public final class FilterOrder {

    public static final int LOGGING = Integer.MIN_VALUE + 1000;
    public static final int SEC_HEADER = Integer.MIN_VALUE + 2000;
    public static final int AUTH = Integer.MIN_VALUE + 3000;
    public static final int AUTHZ = Integer.MIN_VALUE + 4000;

    private FilterOrder() {}
}
```

- [ ] **步骤 2：编写 ShAuthAutoConfiguration**

```java
package com.wkclz.auth;

import com.wkclz.auth.config.AuthProperties;
import com.wkclz.auth.context.SecurityContext;
import com.wkclz.auth.contract.auth.DefaultLogoutService;
import com.wkclz.auth.contract.auth.SessionStore;
import com.wkclz.auth.contract.auth.TokenService;
import com.wkclz.auth.contract.authz.AccessControlProvider;
import com.wkclz.auth.contract.infra.RequestLogger;
import com.wkclz.auth.contract.infra.SecurityHeaderProvider;
import com.wkclz.auth.filter.*;
import org.springframework.boot.autoconfigure.AutoConfiguration;
import org.springframework.boot.autoconfigure.condition.ConditionalOnMissingBean;
import org.springframework.boot.autoconfigure.condition.ConditionalOnProperty;
import org.springframework.boot.context.properties.EnableConfigurationProperties;
import org.springframework.boot.web.servlet.FilterRegistrationBean;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.ComponentScan;

import java.util.List;

/**
 * sh-auth 自动配置
 */
@AutoConfiguration
@ConditionalOnProperty(prefix = "sh.auth", name = "enabled", havingValue = "true", matchIfMissing = true)
@ComponentScan(basePackages = "com.wkclz.auth")
@EnableConfigurationProperties(AuthProperties.class)
public class ShAuthAutoConfiguration {

    @Bean
    @ConditionalOnMissingBean
    public SecurityContext securityContext() {
        return new SecurityContext();
    }

    @Bean
    @ConditionalOnMissingBean
    public DefaultLogoutService defaultLogoutService(SessionStore sessionStore) {
        return new DefaultLogoutService(sessionStore);
    }

    // ===== FilterRegistrationBean 注册 =====

    @Bean
    public FilterRegistrationBean<RequestWrapperFilter> requestWrapperFilterReg() {
        FilterRegistrationBean<RequestWrapperFilter> reg = new FilterRegistrationBean<>();
        reg.setFilter(new RequestWrapperFilter());
        reg.addUrlPatterns("/*");
        reg.setOrder(Integer.MIN_VALUE);
        reg.setName("requestWrapperFilter");
        return reg;
    }

    @Bean
    public FilterRegistrationBean<RequestLogFilter> requestLogFilterReg(List<RequestLogger> requestLoggers) {
        FilterRegistrationBean<RequestLogFilter> reg = new FilterRegistrationBean<>();
        reg.setFilter(new RequestLogFilter(requestLoggers));
        reg.addUrlPatterns("/*");
        reg.setOrder(FilterOrder.LOGGING);
        reg.setName("requestLogFilter");
        return reg;
    }

    @Bean
    @ConditionalOnMissingBean
    public FilterRegistrationBean<SecurityHeaderFilter> securityHeaderFilterReg(
            List<SecurityHeaderProvider> headerProviders) {
        FilterRegistrationBean<SecurityHeaderFilter> reg = new FilterRegistrationBean<>();
        reg.setFilter(new SecurityHeaderFilter(headerProviders));
        reg.addUrlPatterns("/*");
        reg.setOrder(FilterOrder.SEC_HEADER);
        reg.setName("securityHeaderFilter");
        return reg;
    }

    @Bean
    @ConditionalOnMissingBean
    public FilterRegistrationBean<AuthenticationFilter> authenticationFilterReg(
            TokenService tokenService, SessionStore sessionStore, AuthProperties properties) {
        FilterRegistrationBean<AuthenticationFilter> reg = new FilterRegistrationBean<>();
        reg.setFilter(new AuthenticationFilter(tokenService, sessionStore, properties));
        reg.addUrlPatterns("/*");
        reg.setOrder(FilterOrder.AUTH);
        reg.setName("authenticationFilter");
        return reg;
    }

    @Bean
    @ConditionalOnMissingBean
    public FilterRegistrationBean<AuthorizationFilter> authorizationFilterReg(
            List<AccessControlProvider> accessControlProviders, AuthProperties properties) {
        FilterRegistrationBean<AuthorizationFilter> reg = new FilterRegistrationBean<>();
        reg.setFilter(new AuthorizationFilter(accessControlProviders, properties));
        reg.addUrlPatterns("/*");
        reg.setOrder(FilterOrder.AUTHZ);
        reg.setName("authorizationFilter");
        return reg;
    }
}
```

- [ ] **步骤 3：编写 AutoConfiguration.imports**

```
com.wkclz.auth.ShAuthAutoConfiguration
```

- [ ] **步骤 4：验证编译**

```bash
mvn compile -pl sh-auth -am
```

预期：BUILD SUCCESS

- [ ] **步骤 5：Commit**

```bash
git add sh-auth/src/main/java/com/wkclz/auth/ShAuthAutoConfiguration.java sh-auth/src/main/java/com/wkclz/auth/filter/FilterOrder.java sh-auth/src/main/resources/META-INF/
git commit -m "feat: add ShAuthAutoConfiguration with FilterRegistrationBean + FilterOrder + AutoConfiguration.imports"
```

---

### 任务 18：集成编译验证

**目标：** 确保整个 sh-auth 模块可独立编译通过。

- [ ] **步骤 1：执行完整编译**

```bash
mvn compile -pl sh-auth -am
```

预期：BUILD SUCCESS，无任何编译错误或警告。

- [ ] **步骤 2：执行 package（验证打包）**

```bash
mvn package -pl sh-auth -am -DskipTests
```

预期：BUILD SUCCESS，`sh-auth/target/sh-auth-5.1.0-SNAPSHOT.jar` 生成。

---

### 任务 19：单元测试 — SecurityContext

**文件：**
- 创建：`sh-auth/src/test/java/com/wkclz/auth/context/SecurityContextTest.java`

- [ ] **步骤 1：编写测试**

```java
package com.wkclz.auth.context;

import com.wkclz.auth.model.Principal;
import org.junit.jupiter.api.AfterEach;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class SecurityContextTest {

    @AfterEach
    void tearDown() {
        SecurityContext.clear();
    }

    @Test
    void testSetAndGetPrincipal() {
        Principal principal = new Principal();
        principal.setUserCode("user_001");
        principal.setUsername("admin");

        SecurityContext.setPrincipal(principal);

        assertEquals("user_001", SecurityContext.getUserCode());
        assertEquals("admin", SecurityContext.getUsername());
    }

    @Test
    void testGetPrincipalWhenNull() {
        assertNull(SecurityContext.getPrincipal());
        assertNull(SecurityContext.getUserCode());
        assertNull(SecurityContext.getUsername());
    }

    @Test
    void testSetAndGetToken() {
        SecurityContext.setToken("test-token-123");
        assertEquals("test-token-123", SecurityContext.getToken());
    }

    @Test
    void testSetAndGetAppCode() {
        SecurityContext.setAppCode("test-app");
        assertEquals("test-app", SecurityContext.getAppCode());
    }

    @Test
    void testSetAndGetTenantCode() {
        SecurityContext.setTenantCode("test-tenant");
        assertEquals("test-tenant", SecurityContext.getTenantCode());
    }

    @Test
    void testClear() {
        Principal principal = new Principal();
        principal.setUserCode("user_001");
        SecurityContext.setPrincipal(principal);
        SecurityContext.setToken("token");
        SecurityContext.setTenantCode("tenant");
        SecurityContext.setAppCode("app");

        SecurityContext.clear();

        assertNull(SecurityContext.getPrincipal());
        assertNull(SecurityContext.getToken());
        assertNull(SecurityContext.getTenantCode());
        assertNull(SecurityContext.getAppCode());
    }
}
```

- [ ] **步骤 2：运行测试**

```bash
mvn test -pl sh-auth -am -Dtest=SecurityContextTest
```

预期：TESTS PASSED（6/6）

- [ ] **步骤 3：Commit**

```bash
git add sh-auth/src/test/java/com/wkclz/auth/context/SecurityContextTest.java
git commit -m "test: add SecurityContext unit tests"
```

---

### 任务 20：单元测试 — LoginService 模板方法

**文件：**
- 创建：`sh-auth/src/test/java/com/wkclz/auth/contract/LoginServiceTest.java`

- [ ] **步骤 1：编写测试**

```java
package com.wkclz.auth.contract;

import com.wkclz.auth.contract.auth.*;
import com.wkclz.auth.enums.AuthStatus;
import com.wkclz.auth.exception.AccountStatusException;
import com.wkclz.auth.exception.AuthenticationException;
import com.wkclz.auth.exception.RateLimitException;
import com.wkclz.auth.model.*;
import jakarta.servlet.http.HttpServletRequest;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;

import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.ArgumentMatchers.any;
import static org.mockito.ArgumentMatchers.anyString;
import static org.mockito.Mockito.*;

@ExtendWith(MockitoExtension.class)
class LoginServiceTest {

    @Mock
    private AuthenticationProvider authProvider;
    @Mock
    private TokenService tokenService;
    @Mock
    private SessionStore sessionStore;
    @Mock
    private RateLimitChecker rateLimitChecker;
    @Mock
    private CaptchaService captchaService;
    @Mock
    private AccountStatusChecker accountStatusChecker;
    @Mock
    private MfaService mfaService;
    @Mock
    private ConcurrentSessionControl concurrentSessionControl;

    private LoginService loginService;
    private AuthRequest request;
    private Principal principal;
    private AuthToken authToken;

    @BeforeEach
    void setUp() {
        loginService = new TestLoginService(
                authProvider, tokenService, sessionStore, rateLimitChecker,
                captchaService, accountStatusChecker, mfaService, concurrentSessionControl);

        request = new AuthRequest();
        principal = new Principal();
        principal.setUserCode("user_001");
        principal.setUsername("admin");

        authToken = new AuthToken();
        authToken.setTokenValue("test-jwt-token");
    }

    @Test
    void testLoginSuccess() {
        AuthResult authResult = new AuthResult();
        authResult.setStatus(AuthStatus.SUCCESS);
        authResult.setPrincipal(principal);

        when(authProvider.authenticate(any(), any())).thenReturn(authResult);
        when(mfaService.isMfaRequired(anyString())).thenReturn(false);
        when(tokenService.generateToken(principal)).thenReturn(authToken);

        AuthResult result = loginService.login(request, mock(HttpServletRequest.class));

        assertTrue(result.isSuccess());
        assertEquals(authToken, result.getToken());
        verify(rateLimitChecker).recordAttempt(anyString(), eq(true));
        verify(sessionStore).save(any());
    }

    @Test
    void testLoginBadCredentials() {
        when(authProvider.authenticate(any(), any()))
                .thenThrow(new AuthenticationException(null, "密码错误"));

        AuthResult result = loginService.login(request, mock(HttpServletRequest.class));

        assertFalse(result.isSuccess());
        verify(rateLimitChecker).recordAttempt(anyString(), eq(false));
    }

    @Test
    void testLoginRateLimited() {
        doThrow(new RateLimitException("频率超限")).when(rateLimitChecker).check(anyString());

        AuthResult result = loginService.login(request, mock(HttpServletRequest.class));

        assertFalse(result.isSuccess());
        verify(authProvider, never()).authenticate(any(), any());
    }

    @Test
    void testLoginAccountDisabled() {
        AuthResult authResult = new AuthResult();
        authResult.setStatus(AuthStatus.SUCCESS);
        authResult.setPrincipal(principal);

        when(authProvider.authenticate(any(), any())).thenReturn(authResult);
        doThrow(new AccountStatusException(null, "账号已禁用"))
                .when(accountStatusChecker).checkStatus("user_001");

        AuthResult result = loginService.login(request, mock(HttpServletRequest.class));

        assertFalse(result.isSuccess());
    }

    @Test
    void testLoginMfaRequired() {
        AuthResult authResult = new AuthResult();
        authResult.setStatus(AuthStatus.SUCCESS);
        authResult.setPrincipal(principal);

        MfaChallenge challenge = new MfaChallenge();
        challenge.setChallengeId("challenge-001");

        when(authProvider.authenticate(any(), any())).thenReturn(authResult);
        when(mfaService.isMfaRequired(anyString())).thenReturn(true);
        when(mfaService.sendChallenge(anyString(), any())).thenReturn(challenge);

        AuthResult result = loginService.login(request, mock(HttpServletRequest.class));

        assertEquals(AuthStatus.MFA_REQUIRED, result.getStatus());
        assertNotNull(result.getMfaChallenge());
    }

    // Test 子类实现
    static class TestLoginService extends LoginService {
        TestLoginService(AuthenticationProvider ap, TokenService ts, SessionStore ss,
                         RateLimitChecker rlc, CaptchaService cs, AccountStatusChecker asc,
                         MfaService ms, ConcurrentSessionControl csc) {
            this.authProvider = ap;
            this.tokenService = ts;
            this.sessionStore = ss;
            this.rateLimitChecker = rlc;
            this.captchaService = cs;
            this.accountStatusChecker = asc;
            this.mfaService = ms;
            this.concurrentSessionControl = csc;
        }

        @Override
        protected void checkRateLimit(AuthRequest request, HttpServletRequest httpRequest) {
            rateLimitChecker.check(httpRequest.getRemoteAddr());
        }

        @Override
        protected void checkCaptcha(AuthRequest request) {}

        @Override
        protected MfaChallenge checkMfa(Principal principal) {
            if (mfaService.isMfaRequired(principal.getUserCode())) {
                return mfaService.sendChallenge(principal.getUserCode(), null);
            }
            return null;
        }

        @Override
        protected Session createSession(Principal principal, AuthToken token, HttpServletRequest httpRequest) {
            Session session = new Session();
            session.setSubjectId(principal.getUserCode());
            return session;
        }

        @Override
        protected void recordLoginLog(AuthResult result, HttpServletRequest httpRequest) {}
    }
}
```

- [ ] **步骤 2：运行测试**

```bash
mvn test -pl sh-auth -am -Dtest=LoginServiceTest
```

预期：TESTS PASSED（5/5）

- [ ] **步骤 3：Commit**

```bash
git add sh-auth/src/test/java/com/wkclz/auth/contract/LoginServiceTest.java
git commit -m "test: add LoginService template method unit tests (5 cases)"
```

---

### 任务 21：最终验证

- [ ] **步骤 1：完整编译 + 测试**

```bash
mvn clean package -pl sh-auth -am
```

预期：BUILD SUCCESS，所有测试通过。

- [ ] **步骤 2：检查 jar 包内容**

```bash
jar tf sh-auth/target/sh-auth-5.1.0-SNAPSHOT.jar | grep "com/wkclz/sh/auth" | head -30
```

预期：看到所有 SPI 接口、模型、过滤器、枚举等 class 文件。

- [ ] **步骤 3：检查 AutoConfiguration.imports 在 jar 中**

```bash
jar tf sh-auth/target/sh-auth-5.1.0-SNAPSHOT.jar | grep "AutoConfiguration.imports"
```

预期：`META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports`

---

## 自检清单

完成所有任务后，对照设计规格检查：

1. **枚举 (8/8)**：AccountStatus, AuthErrorType, AuthStatus, CredentialType, FieldPermissionType, MenuType, MfaType, TokenType
2. **异常 (7/7)**：AuthException(基类), AuthenticationException, AuthorizationException, SessionExpiredException, AccountStatusException, MfaRequiredException, RateLimitException
3. **模型 (19/19)**：全部已实现（含 AuthMetadata 应用级单例 + SubjectAuthorization 用户级轻量快照）
4. **SPI 接口 (25/25)**：全部已实现，含 DefaultLogoutService 默认实现
5. **LoginService**：模板方法含 7 步流程 + 4 种异常捕获 + recordAttempt
6. **过滤器 (6/6)**：RequestWrapper, RequestLog, SecurityHeader, Authentication, Authorization + FilterOrder 常量
7. **SecurityContext**：含 clear() 方法，RequestLogFilter.finally 中调用
8. **ShAuthAutoConfiguration**：含 FilterRegistrationBean 注册 + DefaultLogoutService Bean
9. **AutoConfiguration.imports**：已配置
10. **零 IAM 依赖**：pom.xml 中不含任何 iam-* 依赖
