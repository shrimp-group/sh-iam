# STORY-016 — 图形验证码接口

| 属性       | 值          |
|----------|------------|
| Story ID | STORY-016  |
| 所属 Epic  | SSO 登录认证模块 |
| 所属模块     | iam-sso    |
| 优先级      | P0         |
| 状态       | 待开发        |

## 用户故事

**作为** 系统用户，**我希望** 在登录页面获取图形验证码，**以便** 在需要时完成人机验证。

## 验收标准

1. API 端点：`GET /iam-sso/public/captcha/chart`
2. 调用 `CaptchaHelper.getCaptcha()` 生成验证码
3. 返回 `PictureCaptchaResp`（captchaId、captchaImage、expireTime）
4. 计算验证码在 Redis 中的 TTL（过期时间 - 当前时间 + 10 秒缓冲）
5. 将 `captchaId -> captchaCode` 存入 Redis，设置 TTL
6. 返回给前端时 captchaCode 置空（不暴露答案）

## 技术实现要点

- 验证码生成委托给 `CaptchaHelper.getCaptcha()`
- Redis Key：`iam:captcha:{captchaId}`
- TTL 计算：`expireTime - System.currentTimeMillis() + 10000`（10 秒缓冲）
- 安全措施：返回前将 captchaCode 置空

## 依赖故事

- STORY-012（图形验证码生成工具）

## 涉及文件

| 文件          | 路径                                                            |
|-------------|---------------------------------------------------------------|
| CaptchaRest | iam-sso/src/main/java/com/wkclz/iam/sso/rest/CaptchaRest.java |
